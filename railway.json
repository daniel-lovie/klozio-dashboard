{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node --experimental-strip-types scripts/migrate.mts && echo \"[boot] migrate done, PORT=$PORT \u2014 starting next\" && exec ./node_modules/.bin/next start -p ${PORT:-3000}",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}